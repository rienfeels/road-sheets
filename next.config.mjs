// next.config.mjs
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // If you used the /app router when creating the project, nothing else is needed.
};

export default nextConfig;
